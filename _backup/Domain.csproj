<Project Sdk="Microsoft.NET.Sdk">

  <!-- Domain thuần BCL (D18): KHÔNG thêm PackageReference (EF Core, Identity, FluentValidation...) vào đây.
       Kiểm tra ràng buộc này bằng architecture test (NFR-MAINT-004). -->
  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <TreatWarningsAsErrors>true</TreatWarningsAsErrors>
  </PropertyGroup>

</Project>
