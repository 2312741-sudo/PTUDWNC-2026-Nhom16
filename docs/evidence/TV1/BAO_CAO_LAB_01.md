# BÁO CÁO LAB - MÔN PHÁT TRIỂN ỨNG DỤNG WEB NÂNG CAO

- **Lab**: 01  
- **Từ ngày**: 09/09/2026 **đến ngày**: 15/09/2026  
- **MSSV**: **2312741**  
- **Họ và tên**: **Nguyễn Thanh Tâm**  
- **Nhóm**: **16** (Nhóm trưởng)  

---

### Bảng công việc thực hiện (Lab 01 / Tuần 1)

| STT | Công việc được giao | Liên kết đến github branch | Tiến độ % |
|:---:|---|---|:---:|
| **1** | **Cài đặt nền tảng xác thực Identity, Roles, Migration, mã hóa mật khẩu PBKDF2 và cấp phát JWT Access Token (Task A1 / FR-AUTH-001/002/006; NFR-SEC-001/002/004)**<br><br>_**Đã hoàn thành:**_<br>- Xây dựng thực thể `ApplicationUser` kế thừa `IdentityUser` và cấu hình DbContext `AuthDbContext` trong tầng Infrastructure.<br>- Cấu hình ASP.NET Core Identity: mã hóa mật khẩu PBKDF2 HMAC-SHA512 (100.000 iterations), kiểm tra độ phức tạp (tối thiểu 8 ký tự, 1 hoa, 1 số, 1 ký tự đặc biệt).<br>- Tạo migration khởi tạo `20260909134304_InitialIdentity`, tạo 8 bảng Identity và seed sẵn 2 roles `Author` và `Admin`.<br>- Cài đặt `JwtService` tạo JWT Access Token (HS256, thời hạn 15 phút) chứa các claim chuẩn (`sub`, `userId`, `email`, `role`, `jti`, `iat`, `exp`).<br>- Soạn thảo tài liệu hợp đồng `docs/AUTH_CONTRACT.md` và kiến trúc `docs/adr/0001-auth-week1.md` bàn giao chuẩn cho các thành viên (TV2, TV3, TV4).<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% mục tiêu Task A1). | [Nhánh Task A1](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/2312741_NguyenThanhTam_Auth-contracts/Identity/JWT,-pipeline/CI;-register/login) | **100%** |
| **2** | **Cài đặt các API Endpoints Đăng ký, Đăng nhập cơ bản và Xem hồ sơ ICurrentUser (Task A2 nền / FR-AUTH-001, FR-AUTH-002, FR-AUTH-006)**<br><br>_**Đã hoàn thành:**_<br>- Triển khai mô hình CQRS qua MediatR: cài đặt `RegisterCommand`, `LoginCommand`, `GetMeQuery` và các Handler tương ứng trong tầng Application.<br>- Xây dựng 3 endpoint Minimal APIs trong Presentation Layer: `POST /api/v1/auth/register`, `POST /api/v1/auth/login`, `GET /api/v1/auth/me`.<br>- Đăng ký tài khoản: tự động cấp role `Author`, cấp Access Token (auto-login), kiểm tra email trùng lặp không phân biệt hoa thường trả về HTTP 409 Conflict.<br>- Chống leo quyền: cấu hình `JsonUnmappedMemberHandling.Disallow` chặn client tự cấp role Admin (HTTP 400).<br>- Đăng nhập: xác thực qua `SignInManager`, bảo mật chống dò tài khoản (trả thông báo lỗi generic 401 khi sai credentials).<br>- Triển khai `HttpCurrentUser` trích xuất UserId an toàn từ claim `sub` của JWT Bearer token.<br>- Viết bộ kiểm thử tích hợp `AuthTests.cs` (10 test cases) xác minh trọn vẹn luồng xác thực.<br><br>_**Chưa hoàn thành:**_<br>- Tính năng khóa tài khoản (Lockout 5 lần) và Rate Limiting 10 req/phút (theo kế hoạch sẽ hoàn thiện ở Tuần 2). | [Nhánh Task A2](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/2312741_NguyenThanhTam_Auth-contracts/Identity/JWT,-pipeline/CI;-register/login) | **100%** |
| **3** | **Chuẩn hóa xử lý lỗi RFC 7807, Logging, CorrelationId và CI Workflow Pipeline (Task A5 nền / FR-OBS-002, NFR-MAINT-001)**<br><br>_**Đã hoàn thành:**_<br>- Triển khai `ApiExceptionHandler` xử lý ngoại lệ tập trung, chuẩn hóa lỗi trả về theo định dạng RFC 7807 Problem Details (`code`, `title`, `detail`, `traceId`, `errors`).<br>- Tích hợp Serilog với Middleware tự động sinh `X-Correlation-ID` và đưa vào Log Context cho mọi request.<br>- Cài đặt MediatR Pipeline Behaviors: `LoggingBehavior` (log thời gian thực thi, cảnh báo khi request > 500ms), `ValidationBehavior` (tự động validate trước khi vào handler).<br>- Cấu hình Redaction: bảo mật tuyệt đối không log mật khẩu, token, security stamp; tắt sensitive logging của EF Core.<br>- Tích hợp giao diện tài liệu OpenAPI & Scalar UI trực quan tại `/scalar/v1`.<br>- Thiết lập quy trình CI tự động qua GitHub Actions (`.github/workflows/backend.yml`) với container PostgreSQL 16, restore locked-mode, build Release, format check và test tự động.<br><br>_**Chưa hoàn thành:**_<br>- Log correlation với tracing phân tán OpenTelemetry (kế hoạch thực hiện ở Tuần 3). | [Nhánh Task A5](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/2312741_NguyenThanhTam_Auth-contracts/Identity/JWT,-pipeline/CI;-register/login) | **100%** |
| **4** | **Thực hiện Lab Kiến trúc Clean Architecture, Domain Value Object và kiểm thử tự động Architecture Tests (Task A6 nền / K01, K03, K04, K21)**<br><br>_**Đã hoàn thành:**_<br>- Soạn thảo tài liệu báo cáo thực hành `docs/evidence/TV1/LAB_KIEN_TRUC_API.md`.<br>- Thiết kế Domain Value Object `DisplayName` kiểm soát nghiệp vụ (1-100 ký tự, auto-trim, chặn ký tự điều khiển và injection HTML/XSS).<br>- Viết bộ kiểm thử kiến trúc `ArchitectureTests.cs` (7 tests) dùng reflection chứng minh tầng Domain và Application độc lập hoàn toàn với Web/EF/Infrastructure.<br>- Đo lường độ phủ kiểm thử (Code Coverage): Tầng Application đạt **100%**, Tầng Domain đạt **100%** (vượt chỉ tiêu yêu cầu ≥ 80%).<br><br>_**Chưa hoàn thành:**_<br>- Các bài lab nâng cao về caching/Redis và Full-Text Search (kế hoạch Tuần 3 & 4). | [Nhánh Task A6](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/2312741_NguyenThanhTam_Auth-contracts/Identity/JWT,-pipeline/CI;-register/login) | **100%** |
| **5** | **Trách nhiệm Nhóm trưởng: Ban hành kế hoạch dự án, phân chia công việc 6 tuần, ban hành Auth Contract và điều phối tích hợp Cổng G0/G1**<br><br>_**Đã hoàn thành:**_<br>- Biên soạn và ban hành `PHAN_CHIA_CONG_VIEC_6_TUAN.md` và `KE_HOACH_DU_AN.md`, định nghĩa rõ ma trận 24 kỹ năng K01–K24 cho 4 thành viên.<br>- Thống nhất hợp đồng giao tiếp giữa các phân hệ: Auth Contract (TV1), Category Contract (TV2), Recipe Schema (TV3), Storage Contract & Docker (TV4).<br>- Hướng dẫn, hỗ trợ các thành viên giải quyết vướng mắc kỹ thuật và seam tích hợp.<br>- Review và phê duyệt các Pull Request Tuần 1: PR #1 & #4 (Hạ tầng, Docker, Nginx của TV4), PR #2 (Auth của TV1), PR #3 (Recipe aggregate schema & Concurrency spike của TV3).<br>- Đảm bảo toàn nhóm đạt 100% chỉ tiêu Cổng G0 (giữa tuần) và Cổng G1 (cuối tuần 1).<br><br>_**Chưa hoàn thành:**_<br>- Không có. | [Pull Requests đã duyệt](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/pulls?q=is%3Apr+is%3Aclosed) | **100%** |
