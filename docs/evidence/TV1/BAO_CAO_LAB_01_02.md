# BÁO CÁO LAB - MÔN PHÁT TRIỂN ỨNG DỤNG WEB NÂNG CAO

- **Lab**: **01 - 02** (Tuần 1 & Tuần 2)
- **Từ ngày**: **09/09/2026** **đến ngày**: **22/09/2026**
- **MSSV**: **2312741**
- **Họ và tên**: **Nguyễn Thanh Tâm**
- **Nhóm**: **16** (Nhóm trưởng — TV1)
- **Phần công việc phụ trách**: Phân hệ Xác thực (Identity & JWT), Hồ sơ & Tài khoản (Profile), Bảo mật hệ thống (Account Lockout, Rate Limiting, Redaction), Xử lý lỗi tập trung (RFC 7807 Problem Details, Logging, CI Pipeline) và Điều phối tổng thể dự án.

---

### Bảng công việc thực hiện (Lab 01 - 02 / Tuần 1 & Tuần 2)

| STT | Công việc được giao | Liên kết đến github branch / PR | Tiến độ % |
|:---:|---|---|:---:|
| **1** | **[Tuần 1] Cài đặt nền tảng xác thực Identity, Roles, Migration, mã hóa mật khẩu PBKDF2 và cấp phát JWT Access Token (Task A1 / FR-AUTH-001/002/006; NFR-SEC-001/002/004)**<br><br>_**Đã hoàn thành:**_<br>- Xây dựng thực thể `ApplicationUser` kế thừa `IdentityUser` và cấu hình DbContext `AuthDbContext` trong tầng Infrastructure.<br>- Cấu hình ASP.NET Core Identity: mã hóa mật khẩu PBKDF2 HMAC-SHA512 (100.000 iterations), kiểm tra độ phức tạp (tối thiểu 8 ký tự, 1 hoa, 1 số, 1 ký tự đặc biệt).<br>- Tạo migration khởi tạo `20260909134304_InitialIdentity`, tạo 8 bảng Identity và seed sẵn 2 roles `Author` và `Admin`.<br>- Cài đặt `JwtService` tạo JWT Access Token (HS256, thời hạn 15 phút) chứa các claim chuẩn (`sub`, `userId`, `email`, `role`, `jti`, `iat`, `exp`).<br>- Soạn thảo tài liệu hợp đồng `docs/AUTH_CONTRACT.md` và kiến trúc `docs/adr/0001-auth-week1.md` bàn giao chuẩn cho các thành viên (TV2, TV3, TV4).<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% mục tiêu Task A1). | [Nhánh Task A1](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/2312741_NguyenThanhTam_Auth-contracts/Identity/JWT,-pipeline/CI;-register/login) | **100%** |
| **2** | **[Tuần 1] Cài đặt các API Endpoints Đăng ký, Đăng nhập cơ bản và Xem hồ sơ ICurrentUser (Task A2 nền / FR-AUTH-001, FR-AUTH-002, FR-AUTH-006)**<br><br>_**Đã hoàn thành:**_<br>- Triển khai mô hình CQRS qua MediatR: cài đặt `RegisterCommand`, `LoginCommand`, `GetMeQuery` và các Handler tương ứng trong tầng Application.<br>- Xây dựng 3 endpoint Minimal APIs trong Presentation Layer: `POST /api/v1/auth/register`, `POST /api/v1/auth/login`, `GET /api/v1/auth/me`.<br>- Đăng ký tài khoản: tự động cấp role `Author`, cấp Access Token (auto-login), kiểm tra email trùng lặp không phân biệt hoa thường trả về HTTP 409 Conflict.<br>- Chống leo quyền: cấu hình `JsonUnmappedMemberHandling.Disallow` chặn client tự cấp role Admin (HTTP 400).<br>- Đăng nhập: xác thực qua `SignInManager`, bảo mật chống dò tài khoản (trả thông báo lỗi generic 401 khi sai credentials).<br>- Triển khai `HttpCurrentUser` trích xuất UserId an toàn từ claim `sub` của JWT Bearer token.<br>- Viết bộ kiểm thử tích hợp `AuthTests.cs` (10 test cases) xác minh trọn vẹn luồng xác thực.<br><br>_**Chưa hoàn thành:**_<br>- Tính năng khóa tài khoản (Lockout 5 lần) và Rate Limiting 10 req/phút (đã chuyển sang hoàn thiện ở Tuần 2). | [Nhánh Task A2](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/2312741_NguyenThanhTam_Auth-contracts/Identity/JWT,-pipeline/CI;-register/login) | **100%** |
| **3** | **[Tuần 1] Chuẩn hóa xử lý lỗi RFC 7807, Logging, CorrelationId và CI Workflow Pipeline (Task A5 nền / FR-OBS-002, NFR-MAINT-001)**<br><br>_**Đã hoàn thành:**_<br>- Triển khai `ApiExceptionHandler` xử lý ngoại lệ tập trung, chuẩn hóa lỗi trả về theo định dạng RFC 7807 Problem Details (`code`, `title`, `detail`, `traceId`, `errors`).<br>- Tích hợp Serilog với Middleware tự động sinh `X-Correlation-ID` và đưa vào Log Context cho mọi request.<br>- Cài đặt MediatR Pipeline Behaviors: `LoggingBehavior` (log thời gian thực thi, cảnh báo khi request > 500ms), `ValidationBehavior` (tự động validate trước khi vào handler).<br>- Cấu hình Redaction: bảo mật tuyệt đối không log mật khẩu, token, security stamp; tắt sensitive logging của EF Core.<br>- Tích hợp giao diện tài liệu OpenAPI & Scalar UI trực quan tại `/scalar/v1`.<br>- Thiết lập quy trình CI tự động qua GitHub Actions (`.github/workflows/backend.yml`) với container PostgreSQL 16, restore locked-mode, build Release, format check và test tự động.<br><br>_**Chưa hoàn thành:**_<br>- Log correlation với tracing phân tán OpenTelemetry (kế hoạch thực hiện ở Tuần 3). | [Nhánh Task A5](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/2312741_NguyenThanhTam_Auth-contracts/Identity/JWT,-pipeline/CI;-register/login) | **100%** |
| **4** | **[Tuần 1] Thực hiện Lab Kiến trúc Clean Architecture, Domain Value Object DisplayName và ArchitectureTests (Task A6 nền / K01, K03, K04, K21)**<br><br>_**Đã hoàn thành:**_<br>- Soạn thảo tài liệu báo cáo thực hành `docs/evidence/TV1/LAB_KIEN_TRUC_API.md`.<br>- Thiết kế Domain Value Object `DisplayName` kiểm soát nghiệp vụ (1-100 ký tự, auto-trim, chặn ký tự điều khiển và injection HTML/XSS).<br>- Viết bộ kiểm thử kiến trúc `ArchitectureTests.cs` (7 tests) dùng reflection chứng minh tầng Domain và Application độc lập hoàn toàn với Web/EF/Infrastructure.<br>- Đo lường độ phủ kiểm thử (Code Coverage): Tầng Application đạt **100%**, Tầng Domain đạt **100%** (vượt chỉ tiêu yêu cầu ≥ 80%).<br><br>_**Chưa hoàn thành:**_<br>- Các bài lab nâng cao về caching/Redis và Full-Text Search (kế hoạch Tuần 3 & 4). | [Nhánh Task A6](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/2312741_NguyenThanhTam_Auth-contracts/Identity/JWT,-pipeline/CI;-register/login) | **100%** |
| **5** | **[Tuần 1] Trách nhiệm Nhóm trưởng: Ban hành kế hoạch dự án, phân chia công việc 6 tuần, ban hành Auth Contract và điều phối tích hợp Cổng G0/G1**<br><br>_**Đã hoàn thành:**_<br>- Biên soạn và ban hành `PHAN_CHIA_CONG_VIEC_6_TUAN.md` và `KE_HOACH_DU_AN.md`, định nghĩa rõ ma trận 24 kỹ năng K01–K24 cho 4 thành viên.<br>- Thống nhất hợp đồng giao tiếp giữa các phân hệ: Auth Contract (TV1), Category Contract (TV2), Recipe Schema (TV3), Storage Contract & Docker (TV4).<br>- Hướng dẫn, hỗ trợ các thành viên giải quyết vướng mắc kỹ thuật và seam tích hợp.<br>- Review và phê duyệt các Pull Request Tuần 1: PR #1 & #4 (Hạ tầng, Docker, Nginx của TV4), PR #2 (Auth của TV1), PR #3 (Recipe aggregate schema & Concurrency spike của TV3).<br>- Đảm bảo toàn nhóm đạt 100% chỉ tiêu Cổng G0 (giữa tuần) và Cổng G1 (cuối tuần 1).<br><br>_**Chưa hoàn thành:**_<br>- Không có. | [Pull Requests đã duyệt](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/pulls?q=is%3Apr+is%3Aclosed) | **100%** |
| **6** | **[Tuần 2] Cơ chế bảo vệ chống brute-force: Khóa tài khoản (Account Lockout 5 lần / 15 phút), Rate Limiting và Generic Credential Error (Task A2 hoàn thiện / FR-AUTH-006, FR-SEC-002, NFR-SEC-004)**<br><br>_**Đã hoàn thành:**_<br>- Cấu hình ASP.NET Core Identity Lockout: `MaxFailedAccessAttempts = 5`, `DefaultLockoutTimeSpan = 15 phút`, `AllowedForNewUsers = true`.<br>- Xử lý trạng thái tài khoản bị khóa trong `IdentityService`: Khi đăng nhập thất bại lần thứ 5 hoặc tài khoản đang bị khóa, hệ thống trả về mã trạng thái HTTP 423 Locked với mã lỗi `"auth.locked"` và thông báo generic `"Email hoặc mật khẩu không đúng."` (chống user enumeration C02 / NFR-SEC-004).<br>- Cấu hình Middleware ASP.NET Core Rate Limiter (`FixedWindowRateLimiter`) tại Program.cs: Giới hạn 10 requests / phút theo Client IP, bảo vệ các endpoint xác thực nhạy cảm `/api/v1/auth/login` và `/api/v1/auth/register`.<br>- Viết 2 bài test tự động chuyên biệt trong `AuthTests.cs`: `Lockout_after_five_failed_attempts_locks_account_and_returns_423` và `Invalid_credentials_return_same_generic_error`.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% chỉ tiêu Task A2). | [Nhánh Task A2](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/2312741_NguyenThanhTam_Auth-contracts/Identity/JWT,-pipeline/CI;-register/login) | **100%** |
| **7** | **[Tuần 2] Xây dựng API và giao diện Hồ sơ người dùng Profile, bảo vệ toàn vẹn tài khoản & Dashboard Shell (Task A3 / FR-AUTH-007, FR-AUTH-008)**<br><br>_**Đã hoàn thành:**_<br>- Tầng Application & Minimal API: Triển khai `UpdateProfileCommand`, `UpdateProfileValidator`, `UpdateProfileHandler` và endpoint `PATCH /api/v1/auth/me`.<br>- Bảo vệ toàn vẹn tài khoản: Chỉ cho phép người dùng cập nhật `displayName`, `avatarUrl`, `bio`. Chặn hoàn toàn việc chỉnh sửa `email` hoặc tự leo quyền `roles` qua API hồ sơ (trả lỗi HTTP 400 BadRequest khi gửi trường không hợp lệ).<br>- Xây dựng giao diện trang cá nhân và quản trị tài khoản tại `/dashboard/profile` trên Next.js 15 App Router: Xem thông tin tài khoản, avatar, huy hiệu vai trò (Role Badge Author/Admin), trạng thái email, ngày tham gia; form cập nhật thông tin kèm thông báo toast/alert thời gian thực.<br>- Viết 4 bài test tự động trong `AuthTests.cs`: `Update_profile_patches_allowed_fields_successfully`, `Update_profile_rejects_unauthorized_call`, `Update_profile_cannot_modify_email_or_roles`, `Update_profile_rejects_xss_and_invalid_inputs`.<br><br>_**Chưa hoàn thành:**_<br>- Tính năng đổi mật khẩu (Change Password) trong dashboard (kế hoạch thực hiện ở Tuần 4). | [Nhánh Task A3](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/2312741_NguyenThanhTam_Auth-contracts/Identity/JWT,-pipeline/CI;-register/login) | **100%** |
| **8** | **[Tuần 2] Xây dựng giao diện Xác thực hiện đại (Đăng nhập, Đăng ký) với Client Validation, Inline Errors & Security Sanitization (Task A4 / FR-AUTH-001, FR-AUTH-002, K05)**<br><br>_**Đã hoàn thành:**_<br>- Xây dựng trang Đăng nhập hoàn chỉnh tại `src/frontend/src/app/auth/login/page.tsx`: Giao diện hiện đại, nút ẩn/hiện mật khẩu, kiểm tra email hợp lệ, hiển thị cảnh báo lỗi (Alert Error) khi sai thông tin hoặc tài khoản bị khóa, tự động lưu token và chuyển hướng về dashboard/trang chủ.<br>- Xây dựng trang Đăng ký hoàn chỉnh tại `src/frontend/src/app/auth/register/page.tsx`: Form gồm Họ và tên, Tên đăng nhập/Email, Mật khẩu, Xác nhận mật khẩu; validate mật khẩu khớp và tối thiểu 8 ký tự; hỗ trợ tự động đăng nhập ngay sau khi đăng ký thành công.<br>- Chuẩn hóa tương thích SRS v1.1.1: Cập nhật thư viện API client (`src/frontend/src/lib/api.ts`) tự động bóc tách vỏ phản hồi `{ data: ... }` (C08).<br>- Tầng backend: `UpdateProfileValidator` kiểm tra nghiêm ngặt độ dài (displayName <= 100, bio <= 2000, avatarUrl <= 500 ký tự HTTP/HTTPS), loại bỏ ký tự điều khiển và chặn injection mã độc HTML/XSS script `<script>`.<br><br>_**Chưa hoàn thành:**_<br>- Đăng nhập bằng Google OAuth PKCE (do TV2 phụ trách bàn giao theo phân công). | [Nhánh Task A4](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/2312741_NguyenThanhTam_Auth-contracts/Identity/JWT,-pipeline/CI;-register/login) | **100%** |
| **9** | **[Tuần 2] Tích hợp cấu hình SMTP Welcome Email job/retry, tối ưu hạ tầng kiểm thử và loại bỏ race condition trên CI (Task A5, A7 / FR-JOB-001, K14, K15, K21)**<br><br>_**Đã hoàn thành:**_<br>- Cấu hình SMTP Mailhog phục vụ gửi email chào mừng (Welcome Email) khi người dùng hoàn tất đăng ký tài khoản (FR-JOB-001), thiết lập cơ chế Hangfire background job với chiến lược tự động retry (1 phút, 5 phút, 30 phút).<br>- Khắc phục lỗi race condition trên CI: Phát hiện lỗi xung đột khởi tạo bảng PostgreSQL khi chạy đồng thời test runner; bổ sung khóa tập trung `ApiFactory.EnsureMigrated()` với `lock (MigrationLock)` và thiết lập `[assembly: CollectionBehavior(DisableTestParallelization = true)]`.<br>- Khắc phục các vi phạm format code C# (.NET code style whitespace / line ending) đáp ứng yêu cầu kiểm tra nghiêm ngặt `dotnet format --verify-no-changes`.<br>- Đạt 54/54 tests backend vượt qua 100% (Green) trên GitHub Actions CI, build frontend Next.js 15 thành công 10/10 routes không có lỗi.<br><br>_**Chưa hoàn thành:**_<br>- Tích hợp giao diện quản lý email queue nâng cao trong Admin Dashboard (kế hoạch Tuần 3-4). | [Nhánh main](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main) | **100%** |
| **10** | **[Tuần 2] Trách nhiệm Nhóm trưởng Tuần 2: Giải quyết triệt để 9 mâu thuẫn hệ thống SRS v1.1.1, ban hành Hướng dẫn Test App và nghiệm thu Cổng G2**<br><br>_**Đã hoàn thành:**_<br>- Chủ trì rà soát toàn bộ tài liệu kỹ thuật, phát hiện và ban hành quyết định giải quyết 9 mâu thuẫn hệ thống (C01–C09): chốt Hard delete cho Category (C07), Soft delete cho Recipe, TTL 60m (C03), chuẩn hóa wrapper response `{ data }` (C08), trường `TimerMinutes` (C04), `OrderIndex` (C05), Name UNIQUE (C09).<br>- Soạn thảo và ban hành tài liệu chính thức: `SRS_Culinary_Blog_v1.1.1.md`, `BAO_CAO_GIAI_QUYET_MAU_THUAN_SRS.md` và `HUONG_DAN_TEST_APP.md` (hướng dẫn chi tiết chạy backend, frontend, database, chạy unit/integration test).<br>- Tiến hành dọn dẹp các tệp tin rác, tệp tin dư thừa không cần thiết trên repository và đồng bộ trạng thái sạch sẽ trên GitHub remote.<br>- Review và nghiệm thu tiến độ của các thành viên Tuần 2: Category Admin CRUD của TV2, Recipe aggregate & RowVersion của TV3, Storage/MinIO & Nginx của TV4; đảm bảo đạt 100% chỉ tiêu Cổng G2.<br><br>_**Chưa hoàn thành:**_<br>- Không có. | [Nhánh main](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main) | **100%** |

---

### Xác nhận hoàn thành và cam kết

- **Tổng số công việc**: 10 đầu việc (5 task Tuần 1 và 5 task Tuần 2)
- **Tiến độ trung bình đạt được**: **100%**
- **Đánh giá cá nhân**: Đã hoàn thành vượt tiến độ yêu cầu của Cổng G1 (Tuần 1) và Cổng G2 (Tuần 2). Toàn bộ mã nguồn backend, frontend, cơ sở dữ liệu migration và 54 automated test cases đều chạy pass 100% trên cả môi trường cục bộ và GitHub Actions CI.
