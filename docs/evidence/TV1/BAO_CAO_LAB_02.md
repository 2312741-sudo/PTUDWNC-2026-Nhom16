# BÁO CÁO LAB - MÔN PHÁT TRIỂN ỨNG DỤNG WEB NÂNG CAO

- **Lab**: **02** (Tuần 2)
- **Từ ngày**: **16/09/2026** **đến ngày**: **22/09/2026**
- **MSSV**: **2312741**
- **Họ và tên**: **Nguyễn Thanh Tâm**
- **Nhóm**: **16** (Nhóm trưởng — TV1)
- **Tên file nộp theo quy định**: `Lab02_2312741_NguyenThanhTam.docx`

---

### Bảng công việc thực hiện (Lab 02 / Tuần 2)

| STT | Công việc được giao theo Yêu cầu Tối thiểu Lab 2 | Liên kết đến github branch | Tiến độ % |
|:---:|---|---|:---:|
| **1** | **Tạo cấu trúc dự án backend theo Clean Architecture**<br><br>_**Đã hoàn thành:**_<br>- Tổ chức giải pháp theo kiến trúc Clean Architecture gồm 4 tầng dự án độc lập:<br>  + `CulinaryBlog.Domain`: Chứa các thực thể cốt lõi (`Category`, `Recipe`, `RecipeIngredient`, `RecipeStep`, `RecipeNutrition`, `RecipeImage`), Enums, Value Objects và `BaseEntity`. Hoàn toàn không phụ thuộc framework ngoài.<br>  + `CulinaryBlog.Application`: Chứa mô hình CQRS qua MediatR (Commands, Queries, Handlers), DTOs, FluentValidation Validators và MediatR Pipeline Behaviors.<br>  + `CulinaryBlog.Infrastructure`: Cài đặt EF Core DbContext, các cấu hình Fluent API Entity Configurations, ASP.NET Core Identity, Repositories và Migrations.<br>  + `CulinaryBlog.API`: Tầng khởi chạy ứng dụng (Minimal APIs, Routing, Middlewares, Exception Handler theo RFC 7807, OpenAPI & Scalar UI).<br>- Xây dựng bộ kiểm thử kiến trúc tự động trong `ArchitectureTests.cs` (18 tests) dùng Reflection chứng minh Domain và Application độc lập hoàn toàn với Web/EF Core/Infrastructure.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Mã nguồn backend](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend) | **100%** |
| **2** | **Cài đặt các gói thư viện cần thiết**<br><br>_**Đã hoàn thành:**_<br>- Cài đặt và cấu hình đầy đủ các thư viện phụ thuộc theo tiêu chuẩn .NET 10:<br>  + Entity Framework Core & PostgreSQL: `Microsoft.EntityFrameworkCore` (v10.0.12), `Microsoft.EntityFrameworkCore.Relational`, `Microsoft.EntityFrameworkCore.Design`, `Npgsql.EntityFrameworkCore.PostgreSQL` (v10.0.3).<br>  + Xác thực & Bảo mật: `Microsoft.AspNetCore.Identity.EntityFrameworkCore` (v10.0.12), `Microsoft.AspNetCore.Authentication.JwtBearer` (v10.0.3), `System.IdentityModel.Tokens.Jwt`.<br>  + CQRS & Validation: `MediatR` (v12.4.1), `FluentValidation.DependencyInjectionExtensions` (v11.11.0).<br>  + Logging & Tài liệu API: `Serilog.AspNetCore`, `Scalar.AspNetCore`, `Microsoft.AspNetCore.OpenApi`.<br>- Khóa toàn bộ dependencies bằng `packages.lock.json` đảm bảo quá trình build CI đạt chuẩn locked-mode không phát sinh lỗi phiên bản.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Mã nguồn backend](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend) | **100%** |
| **3** | **Cài đặt các lớp entities, configuration, dbcontext**<br><br>_**Đã hoàn thành:**_<br>- Cài đặt hệ thống Entities trong tầng Domain: `Category` (danh mục), `Recipe` (công thức aggregate root), `RecipeIngredient` (nguyên liệu), `RecipeStep` (bước thực hiện với TimerMinutes), `RecipeNutrition` (giá trị dinh dưỡng), `RecipeImage` (hình ảnh), `RecipeAuthorUser` (thực thể tác giả kế thừa IdentityUser).<br>- Cài đặt 7 lớp Entity Configuration trong `Persistence/Configurations`: `CategoryConfiguration`, `RecipeConfiguration`, `RecipeIngredientConfiguration`, `RecipeStepConfiguration`, `RecipeImageConfiguration`, `RecipeAuthorUserConfiguration`, `RefreshTokenConfiguration`.<br>- Thiết lập các ràng buộc toàn vẹn dữ liệu: khóa chính, khóa ngoại, các chỉ mục Index (`Slug`, `Name` UNIQUE, `Difficulty`, `Status`, `IsDeleted`), kiểm soát tương tranh lạc quan `RowVersion` (xmin).<br>- Cài đặt `ApplicationDbContext` kế thừa `IdentityDbContext<RecipeAuthorUser>` và hiện thực hóa `IApplicationDbContext`, tích hợp Global Query Filter tự động lọc bỏ các bản ghi đã xóa mềm (`IsDeleted`).<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Persistence Infrastructure](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.Infrastructure/Persistence) | **100%** |
| **4** | **Tạo migration, cài đặt các lớp để tạo dữ liệu ngẫu nhiên**<br><br>_**Đã hoàn thành:**_<br>- Tạo file migration EF Core hoàn chỉnh `20260914061426_InitialCreate.cs` định nghĩa cấu trúc 14 bảng trong PostgreSQL.<br>- Cài đặt lớp sinh dữ liệu ngẫu nhiên `DbSeeder.cs`: sử dụng thuật toán giả lập dữ liệu thực tế cho 100 món ăn truyền thống và hiện đại của ẩm thực Việt Nam.<br>- Bộ sinh dữ liệu tự động gắn kết danh mục, tác giả, tự tính toán bảng dinh dưỡng (Calories, Protein, Carbs, Fat, Fiber, Sodium), thêm ảnh đại diện và phân bổ trạng thái Published/Draft.<br>- Tích hợp cờ lệnh dòng lệnh `--migrate` và `--seed` trong `Program.cs` của dự án `CulinaryBlog.API` cho phép áp dụng migration và nạp dữ liệu tức thì.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [DbSeeder.cs](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.Infrastructure/Persistence/DbSeeder.cs) | **100%** |
| **5** | **Đảm bảo đã tạo được cơ sở dữ liệu có chứa dữ liệu ngẫu nhiên cho ít nhất 20 categories, 100 recipes (mỗi recipe có ít nhất 10 nguyên liệu và ít nhất 5 bước chế biến)**<br><br>_**Đã hoàn thành:**_<br>- Đã thực thi migration và nạp dữ liệu mẫu thành công vào cơ sở dữ liệu PostgreSQL (database `culinary_blog`).<br>- Kiểm chứng số lượng dữ liệu thực tế bằng truy vấn SQL (`SELECT COUNT`) đạt và vượt chỉ tiêu yêu cầu:<br>  + **Số lượng Categories**: **25 danh mục** (yêu cầu tối thiểu ≥ 20).<br>  + **Số lượng Recipes**: **100 công thức ẩm thực** (yêu cầu tối thiểu ≥ 100).<br>  + **Số lượng RecipeIngredients**: **1099 nguyên liệu** (mỗi recipe có từ 10 đến 12 nguyên liệu cụ thể, yêu cầu tối thiểu ≥ 10).<br>  + **Số lượng RecipeSteps**: **550 bước chế biến** (mỗi recipe có từ 5 đến 6 bước chi tiết kèm thời gian `TimerMinutes`, yêu cầu tối thiểu ≥ 5).<br>  + **Số lượng Tác giả (Users)**: **5 tài khoản tác giả ẩm thực** đại diện cho các thành viên trong nhóm.<br>  + **Tỷ lệ xuất bản**: 85 công thức Published và 15 công thức Draft để phục vụ kiểm thử trạng thái.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [DbSeeder.cs](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.Infrastructure/Persistence/DbSeeder.cs) | **100%** |

---

### Xác nhận hoàn thành

- Toàn bộ **5/5 yêu cầu tối thiểu của Lab 2 (tuần 2)** đã được thực hiện và kiểm chứng thực tế trên cơ sở dữ liệu PostgreSQL.
- File báo cáo nộp bài theo chuẩn: `Lab02_2312741_NguyenThanhTam.docx` đã được tạo tại thư mục `~/Downloads/`.
