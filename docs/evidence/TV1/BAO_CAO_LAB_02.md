# BÁO CÁO LAB - MÔN PHÁT TRIỂN ỨNG DỤNG WEB NÂNG CAO

- **Lab**: **02 (Bao gồm Tuần 1 & Tuần 2 — Lần nộp đầu tiên)**
- **Từ ngày**: **09/09/2026** **đến ngày**: **22/09/2026**
- **MSSV**: **2312741**
- **Họ và tên**: **Nguyễn Thanh Tâm**
- **Nhóm**: **16** (Nhóm trưởng — TV1)
- **Tên file nộp theo quy định**: `Lab02_2312741_NguyenThanhTam.docx`

---

### Bảng công việc thực hiện (Tổng hợp Tuần 1 & Tuần 2 / Lab 01 & Lab 02)

| STT | Công việc được giao | Liên kết đến github branch / PR / file | Tiến độ % |
|:---:|---|---|:---:|
| **1** | **[Yêu cầu Lab 2 & Tuần 1] Thiết kế cấu trúc dự án backend theo Clean Architecture & Kiểm định Architecture Tests**<br><br>_**Đã hoàn thành:**_<br>- Tổ chức giải pháp theo kiến trúc Clean Architecture gồm 4 tầng dự án độc lập:<br>  + `CulinaryBlog.Domain`: Chứa các thực thể cốt lõi (`Category`, `Recipe`, `RecipeIngredient`, `RecipeStep`, `RecipeNutrition`, `RecipeImage`), Enums, Value Objects (`DisplayName`) và `BaseEntity`. Hoàn toàn không phụ thuộc framework ngoài.<br>  + `CulinaryBlog.Application`: Chứa mô hình CQRS qua MediatR (Commands, Queries, Handlers), DTOs, FluentValidation Validators và MediatR Pipeline Behaviors (`LoggingBehavior`, `ValidationBehavior`).<br>  + `CulinaryBlog.Infrastructure`: Cài đặt EF Core DbContext, các cấu hình Fluent API Entity Configurations, ASP.NET Core Identity, Repositories và Migrations.<br>  + `CulinaryBlog.API`: Tầng khởi chạy ứng dụng (Minimal APIs, Routing, Middlewares, Exception Handler theo RFC 7807, OpenAPI & Scalar UI).<br>- Xây dựng bộ kiểm thử kiến trúc tự động trong `ArchitectureTests.cs` (18 tests) dùng Reflection chứng minh Domain và Application độc lập hoàn toàn với Web/EF Core/Infrastructure. Độ phủ kiểm thử tầng Domain và Application đạt 100%.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Mã nguồn backend](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend) | **100%** |
| **2** | **[Yêu cầu Lab 2 & Tuần 1] Cài đặt các gói thư viện cần thiết và quản lý dependencies locked-mode**<br><br>_**Đã hoàn thành:**_<br>- Cài đặt và cấu hình đầy đủ các thư viện phụ thuộc theo tiêu chuẩn .NET 10:<br>  + Entity Framework Core & PostgreSQL: `Microsoft.EntityFrameworkCore` (v10.0.12), `Microsoft.EntityFrameworkCore.Relational`, `Microsoft.EntityFrameworkCore.Design`, `Npgsql.EntityFrameworkCore.PostgreSQL` (v10.0.3).<br>  + Xác thực & Bảo mật: `Microsoft.AspNetCore.Identity.EntityFrameworkCore` (v10.0.12), `Microsoft.AspNetCore.Authentication.JwtBearer` (v10.0.3), `System.IdentityModel.Tokens.Jwt`.<br>  + CQRS & Validation: `MediatR` (v12.4.1), `FluentValidation.DependencyInjectionExtensions` (v11.11.0).<br>  + Logging & Tài liệu API: `Serilog.AspNetCore`, `Scalar.AspNetCore`, `Microsoft.AspNetCore.OpenApi`, `Microsoft.Extensions.Diagnostics.HealthChecks`.<br>- Khóa toàn bộ dependencies bằng `packages.lock.json` đảm bảo quá trình khôi phục (`dotnet restore --locked-mode`) và build trên CI đạt tính nhất quán tuyệt đối, không phát sinh lỗi phiên bản.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Mã nguồn backend](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend) | **100%** |
| **3** | **[Yêu cầu Lab 2 & Tuần 1-2] Cài đặt các lớp Entities, Configuration và DbContext (ApplicationDbContext & AuthDbContext)**<br><br>_**Đã hoàn thành:**_<br>- Cài đặt hệ thống Entities trong tầng Domain: `Category` (danh mục), `Recipe` (công thức aggregate root), `RecipeIngredient` (nguyên liệu), `RecipeStep` (bước thực hiện với TimerMinutes), `RecipeNutrition` (giá trị dinh dưỡng), `RecipeImage` (hình ảnh), `RecipeAuthorUser` và `ApplicationUser` (kế thừa `IdentityUser`).<br>- Cài đặt 7 lớp Entity Configuration trong `Persistence/Configurations`: `CategoryConfiguration`, `RecipeConfiguration`, `RecipeIngredientConfiguration`, `RecipeStepConfiguration`, `RecipeImageConfiguration`, `RecipeAuthorUserConfiguration`, `RefreshTokenConfiguration`.<br>- Thiết lập các ràng buộc toàn vẹn dữ liệu: khóa chính, khóa ngoại, các chỉ mục Index (`Slug`, `Name` UNIQUE, `Difficulty`, `Status`, `IsDeleted`), kiểm soát tương tranh lạc quan `RowVersion` (xmin).<br>- Cài đặt `ApplicationDbContext` kế thừa `IdentityDbContext<RecipeAuthorUser>` và hiện thực hóa `IApplicationDbContext`, tích hợp Global Query Filter tự động lọc bỏ các bản ghi đã xóa mềm (`IsDeleted`). Đồng thời duy trì `AuthDbContext` cho phân hệ xác thực Identity.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Persistence Infrastructure](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.Infrastructure/Persistence) | **100%** |
| **4** | **[Yêu cầu Lab 2 & Tuần 2] Tạo Migration và cài đặt lớp sinh dữ liệu ngẫu nhiên (DbSeeder.cs)**<br><br>_**Đã hoàn thành:**_<br>- Tạo file migration EF Core hoàn chỉnh `20260914061426_InitialCreate.cs` định nghĩa cấu trúc 14 bảng quan hệ và chỉ mục trong PostgreSQL.<br>- Cài đặt lớp sinh dữ liệu ngẫu nhiên `DbSeeder.cs`: sử dụng thuật toán giả lập dữ liệu thực tế cho 100 món ăn truyền thống và hiện đại của ẩm thực Việt Nam.<br>- Bộ sinh dữ liệu tự động gắn kết danh mục, tác giả, tự tính toán bảng dinh dưỡng (Calories, Protein, Carbs, Fat, Fiber, Sodium), thêm ảnh đại diện và phân bổ trạng thái Published/Draft.<br>- Tích hợp cờ lệnh dòng lệnh `--migrate` và `--seed` trong `Program.cs` của dự án `CulinaryBlog.API` cho phép áp dụng migration và nạp dữ liệu tức thì qua CLI.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [DbSeeder.cs](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.Infrastructure/Persistence/DbSeeder.cs) | **100%** |
| **5** | **[Yêu cầu Lab 2 & Tuần 2] Đảm bảo CSDL chứa dữ liệu ngẫu nhiên: 25 categories, 100 recipes (mỗi recipe >= 10 nguyên liệu, >= 5 bước)**<br><br>_**Đã hoàn thành:**_<br>- Đã thực thi migration và nạp dữ liệu mẫu thành công vào cơ sở dữ liệu PostgreSQL (database `culinary_blog`).<br>- Kiểm chứng số lượng dữ liệu thực tế bằng truy vấn SQL (`SELECT COUNT`) đạt và vượt chỉ tiêu yêu cầu của đề bài:<br>  + **Số lượng Categories**: **25 danh mục ẩm thực phong phú** (yêu cầu tối thiểu ≥ 20).<br>  + **Số lượng Recipes**: **100 công thức ẩm thực chi tiết** (yêu cầu tối thiểu ≥ 100).<br>  + **Số lượng RecipeIngredients**: **1.099 nguyên liệu** (mỗi recipe có từ 10 đến 12 nguyên liệu cụ thể, yêu cầu tối thiểu ≥ 10).<br>  + **Số lượng RecipeSteps**: **550 bước chế biến** (mỗi recipe có từ 5 đến 6 bước chi tiết kèm thời gian `TimerMinutes`, yêu cầu tối thiểu ≥ 5).<br>  + **Số lượng Tác giả (Users)**: **5 tài khoản tác giả ẩm thực** đại diện cho các thành viên trong nhóm.<br>  + **Tỷ lệ xuất bản**: 85 công thức Published và 15 công thức Draft để phục vụ kiểm thử trạng thái.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [DbSeeder.cs](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.Infrastructure/Persistence/DbSeeder.cs) | **100%** |
| **6** | **[Tuần 1] Nền tảng Xác thực Identity, Roles, mã hóa PBKDF2 HMAC-SHA512 và cấp phát JWT Access Token (Task A1 / FR-AUTH-001/002/006)**<br><br>_**Đã hoàn thành:**_<br>- Xây dựng thực thể `ApplicationUser` kế thừa `IdentityUser` và cấu hình DbContext `AuthDbContext` trong tầng Infrastructure.<br>- Cấu hình ASP.NET Core Identity: mã hóa mật khẩu PBKDF2 HMAC-SHA512 (100.000 iterations), kiểm tra độ phức tạp (tối thiểu 8 ký tự, 1 hoa, 1 số, 1 ký tự đặc biệt).<br>- Tạo migration khởi tạo `20260909134304_InitialIdentity`, tạo 8 bảng Identity và seed sẵn 2 roles `Author` và `Admin`.<br>- Cài đặt `JwtService` tạo JWT Access Token (HS256, thời hạn 15 phút) chứa các claim chuẩn (`sub`, `userId`, `email`, `role`, `jti`, `iat`, `exp`).<br>- Soạn thảo tài liệu hợp đồng `docs/AUTH_CONTRACT.md` và kiến trúc `docs/adr/0001-auth-week1.md` bàn giao chuẩn cho các thành viên (TV2, TV3, TV4).<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Mã nguồn backend](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend) | **100%** |
| **7** | **[Tuần 1 & 2] Cài đặt API Endpoints Đăng ký, Đăng nhập, ICurrentUser và Bảo vệ chống brute-force (Lockout 5 lần, Rate Limit) (Task A2)**<br><br>_**Đã hoàn thành:**_<br>- Triển khai mô hình CQRS qua MediatR: cài đặt `RegisterCommand`, `LoginCommand`, `GetMeQuery` và các Handler tương ứng trong tầng Application.<br>- Xây dựng 3 endpoint Minimal APIs trong Presentation Layer: `POST /api/v1/auth/register`, `POST /api/v1/auth/login`, `GET /api/v1/auth/me`.<br>- Đăng ký tài khoản: tự động cấp role `Author`, cấp Access Token (auto-login), kiểm tra email trùng lặp không phân biệt hoa thường trả về HTTP 409 Conflict. Chống leo quyền Admin (Disallow).<br>- Cơ chế Khóa tài khoản (Account Lockout): Khóa 15 phút sau 5 lần đăng nhập sai liên tiếp, trả HTTP 423 Locked với mã lỗi `'auth.locked'` và thông báo generic `'Email hoặc mật khẩu không đúng.'` (chống dò tài khoản C02).<br>- Tích hợp Middleware ASP.NET Core Rate Limiter giới hạn 10 requests/phút/IP bảo vệ các endpoint xác thực nhạy cảm.<br>- Viết bộ kiểm thử tích hợp chuyên biệt trong `AuthTests.cs` xác minh trọn vẹn luồng xác thực và bảo mật chống brute-force.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Mã nguồn backend](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend) | **100%** |
| **8** | **[Tuần 2] Xây dựng API và giao diện Hồ sơ người dùng Profile, bảo vệ toàn vẹn tài khoản & Dashboard Shell (Task A3 / FR-AUTH-007, FR-AUTH-008)**<br><br>_**Đã hoàn thành:**_<br>- Tầng Application & Minimal API: Triển khai `UpdateProfileCommand`, `UpdateProfileValidator`, `UpdateProfileHandler` và endpoint `PATCH /api/v1/auth/me`.<br>- Bảo vệ toàn vẹn tài khoản: Chỉ cho phép người dùng cập nhật `displayName`, `avatarUrl`, `bio`. Chặn hoàn toàn việc chỉnh sửa email hoặc tự leo quyền roles qua API hồ sơ (trả lỗi HTTP 400 BadRequest khi gửi trường không hợp lệ).<br>- Xây dựng giao diện trang cá nhân và quản trị tài khoản tại `/dashboard/profile` trên Next.js 15 App Router: Xem thông tin tài khoản, avatar, huy hiệu vai trò (Role Badge Author/Admin), trạng thái email, ngày tham gia; form cập nhật thông tin kèm thông báo toast/alert thời gian thực.<br>- Viết 4 bài test tự động trong `AuthTests.cs`: `Update_profile_patches_allowed_fields_successfully`, `Update_profile_rejects_unauthorized_call`, `Update_profile_cannot_modify_email_or_roles`, `Update_profile_rejects_xss_and_invalid_inputs`.<br><br>_**Chưa hoàn thành:**_<br>- Tính năng đổi mật khẩu (Change Password) trong dashboard (kế hoạch thực hiện ở Tuần 4). | [Giao diện Profile](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/frontend/src/app/dashboard/profile) | **100%** |
| **9** | **[Tuần 1 & 2] Xử lý lỗi RFC 7807 Problem Details, Logging, CorrelationId, Welcome Email Worker và CI Workflow Pipeline (Task A5/A7)**<br><br>_**Đã hoàn thành:**_<br>- Triển khai `ApiExceptionHandler` xử lý ngoại lệ tập trung, chuẩn hóa lỗi trả về theo định dạng RFC 7807 Problem Details (`code`, `title`, `detail`, `traceId`, `errors`).<br>- Tích hợp Serilog với Middleware tự động sinh `X-Correlation-ID` và đưa vào Log Context cho mọi request. Cài đặt MediatR `LoggingBehavior` và `ValidationBehavior`.<br>- Cấu hình Redaction: bảo mật tuyệt đối không log mật khẩu, token, security stamp; tắt sensitive logging của EF Core.<br>- Cấu hình SMTP Mailhog gửi email chào mừng qua `System.Threading.Channels` bất đồng bộ, cơ chế retry tự động (1m, 5m, 30m) không làm rollback transaction đăng ký.<br>- Thiết lập quy trình CI tự động qua GitHub Actions (`.github/workflows/backend.yml`), khắc phục lỗi race condition trên PostgreSQL test runner, đảm bảo toàn bộ 77/77 tests pass 100% (Green).<br><br>_**Chưa hoàn thành:**_<br>- Log correlation với tracing phân tán OpenTelemetry đầy đủ (kế hoạch thực hiện ở Tuần 3). | [GitHub Actions CI](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/actions) | **100%** |
| **10** | **[Tuần 1 & 2] Trách nhiệm Nhóm trưởng: Kế hoạch 6 tuần, Auth Contract, Giải quyết 9 mâu thuẫn SRS v1.1.1, Hướng dẫn test app & Nghiệm thu G1/G2**<br><br>_**Đã hoàn thành:**_<br>- Biên soạn và ban hành `PHAN_CHIA_CONG_VIEC_6_TUAN.md` và `KE_HOACH_DU_AN.md`, định nghĩa rõ ma trận 24 kỹ năng K01–K24 cho 4 thành viên.<br>- Thống nhất hợp đồng giao tiếp giữa các phân hệ: Auth Contract (TV1), Category Contract (TV2), Recipe Schema (TV3), Storage Contract & Docker (TV4).<br>- Chủ trì rà soát toàn bộ tài liệu kỹ thuật, phát hiện và ban hành quyết định giải quyết 9 mâu thuẫn hệ thống (C01–C09: hard delete Category, soft delete Recipe, TTL 60m, { data }, TimerMinutes, OrderIndex).<br>- Ban hành tài liệu chính thức: `SRS_Culinary_Blog_v1.1.1.md`, `BAO_CAO_GIAI_QUYET_MAU_THUAN_SRS.md` và `HUONG_DAN_TEST_APP.md`.<br>- Review và phê duyệt toàn bộ Pull Requests của nhóm; nghiệm thu 100% Cổng G0, G1 và điều phối Cổng G2.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Pull Requests](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/pulls?q=is%3Apr+is%3Aclosed) | **100%** |

---

### Xác nhận hoàn thành

- Toàn bộ **5/5 yêu cầu tối thiểu của Lab 2 (tuần 2)** cùng **toàn bộ các nhiệm vụ chức năng & vai trò Nhóm trưởng của Tuần 1 và Tuần 2** đã được thực hiện và kiểm chứng thực tế (100%).
- File báo cáo nộp bài theo chuẩn: `Lab02_2312741_NguyenThanhTam.docx` đã được tạo tại thư mục `~/Downloads/` và lưu trữ tại `docs/evidence/TV1/Lab02_2312741_NguyenThanhTam.docx`.
- File mẫu gốc `~/Downloads/Mau_Nop_Bao_Cao_Lab_Ca_Nhan_2026.docx` cũng đã được cập nhật đồng bộ.
