# BÁO CÁO LAB - MÔN PHÁT TRIỂN ỨNG DỤNG WEB NÂNG CAO

- **Lab**: **03 (Tuần 3 — Hoàn thiện Xác thực, Token Rotation, Cache/SEO & Nghiệm thu G3)**
- **Từ ngày**: **16/09/2026** **đến ngày**: **23/09/2026**
- **MSSV**: **2312741**
- **Họ và tên**: **Nguyễn Thanh Tâm**
- **Nhóm**: **16** (Nhóm trưởng — TV1)
- **Tên file nộp theo quy định**: `Lab03_2312741_NguyenThanhTam.docx`

---

### Bảng công việc thực hiện (Lab 03 / Tuần 3)

| STT | Công việc được giao | Liên kết đến github branch / PR / file | Tiến độ % |
|:---:|---|---|:---:|
| **1** | **[Nền tảng Kiến trúc & CSDL] Duy trì Clean Architecture, Dependencies locked-mode và CSDL hạt giống đạt chuẩn**<br><br>_**Đã hoàn thành:**_<br>- Duy trì kiến trúc 4 tầng phân tách độc lập (`Domain`, `Application`, `Infrastructure`, `API`), 18 tests kiến trúc tự động chứng minh không vi phạm phụ thuộc.<br>- Quản lý thư viện phụ thuộc chế độ khóa `packages.lock.json` đảm bảo quá trình build trên CI đạt tính nhất quán tuyệt đối.<br>- Cơ sở dữ liệu PostgreSQL thực tế lưu trữ 25 categories, 100 recipes, 1.099 ingredients, 550 steps kèm thời gian `TimerMinutes` và bảng dinh dưỡng đầy đủ.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Mã nguồn backend](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend) | **100%** |
| **2** | **[Bảo mật Xác thực Cơ bản] Nền tảng Identity, PBKDF2 HMAC-SHA512, JWT Access Token, Lockout 5 lần & Rate Limiter**<br><br>_**Đã hoàn thành:**_<br>- Cấu hình ASP.NET Core Identity với mã hóa mật khẩu PBKDF2 HMAC-SHA512 (100.000 iterations).<br>- Cấp phát JWT Access Token (HS256, thời hạn 15 phút) chứa đầy đủ các claim chuẩn (`sub`, `userId`, `email`, `role`, `jti`, `iat`, `exp`).<br>- Triển khai API Đăng ký (`POST /api/v1/auth/register`) và Đăng nhập (`POST /api/v1/auth/login`).<br>- Cơ chế Account Lockout: Khóa 15 phút sau 5 lần nhập sai liên tiếp (trả mã HTTP 423 `auth.locked`).<br>- Middleware Rate Limiting: Giới hạn 10 requests/phút/IP bảo vệ các endpoint xác thực nhạy cảm.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Auth Endpoints](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.API/Program.cs) | **100%** |
| **3** | **[Hồ sơ & Giao diện Dashboard] API Profile, bảo vệ toàn vẹn tài khoản và Giao diện Dashboard Shell**<br><br>_**Đã hoàn thành:**_<br>- API cập nhật hồ sơ (`PATCH /api/v1/auth/me`): Cho phép cập nhật `displayName`, `avatarUrl`, `bio`; chặn hoàn toàn việc chỉnh sửa `email` hoặc tự nâng quyền `roles`.<br>- Xây dựng giao diện trang cá nhân và quản trị tài khoản tại `/dashboard/profile` trên Next.js 15 App Router: Hiển thị avatar, badge vai trò (Author/Admin), trạng thái email xác thực và form cập nhật hồ sơ.<br>- Bộ kiểm thử `AuthTests.cs` xác nhận kiểm soát dữ liệu đầu vào và chống tấn công XSS/HTML Injection.<br><br>_**Chưa hoàn thành:**_<br>- Tính năng đổi mật khẩu (kế hoạch Tuần 4). | [Giao diện Profile](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/frontend/src/app/dashboard/profile) | **100%** |
| **4** | **[Nền tảng Refresh Token & Token Rotation (Task A5/C5)] Cấp phát Refresh Token 512-bit, Rotation và Family Reuse Revocation**<br><br>_**Đã hoàn thành:**_<br>- Thiết kế và tạo bảng `RefreshTokens` trong PostgreSQL với chỉ mục duy nhất `IDX_RefreshToken_Hash`.<br>- Sinh Refresh Token ngẫu nhiên mật mã 512-bit (`RandomNumberGenerator`), chỉ lưu trữ bản băm SHA-256 (64 hex characters) trong CSDL, không lưu raw token.<br>- Triển khai endpoint `POST /api/v1/auth/refresh` và xử lý **Token Rotation**: Mỗi lần làm mới sẽ thu hồi token cũ và cấp cặp Access Token + Refresh Token mới, liên kết vết qua `ReplacedByTokenHash`.<br>- Cơ chế **Token Reuse Detection / Family Revocation**: Khi phát hiện một token đã bị thu hồi cố tình được sử dụng lại (Replay Attack), hệ thống lập tức thu hồi toàn bộ các token đang hoạt động của người dùng đó (Family Revocation), vô hiệu hóa hoàn toàn phiên làm việc của kẻ tấn công.<br>- Viết 4 bài test tự động chuyên biệt trong `Week3AuthAndPersonalLabTests.cs` kiểm chứng trọn vẹn luồng cấp phát, xoay vòng và phòng chống replay attack.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [IdentityService.cs](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.Infrastructure/IdentityService.cs) | **100%** |
| **5** | **[Cơ chế Đăng xuất & Thu hồi phiên làm việc (Task D3)] API Logout thu hồi Refresh Token và hủy phiên an toàn**<br><br>_**Đã hoàn thành:**_<br>- Triển khai endpoint `POST /api/v1/auth/logout` nhận `LogoutCommand(string? RefreshToken)`.<br>- Khi người dùng đăng xuất, hệ thống kiểm tra và thu hồi (`Revoke`) Refresh Token tương ứng trong cơ sở dữ liệu.<br>- Nếu không truyền token cụ thể, hệ thống thu hồi toàn bộ token đang hoạt động của người dùng hiện tại.<br>- Viết bài test tự động xác nhận sau khi logout thì Refresh Token cũ bị từ chối `401 Unauthorized` ngay lập tức.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Auth.cs](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.Application/Auth.cs) | **100%** |
| **6** | **[Quan sát hệ thống & An toàn dữ liệu (Task A5/K20)] W3C Tracing, X-Correlation-ID, Serilog Redaction và Health Checks**<br><br>_**Đã hoàn thành:**_<br>- Tích hợp middleware tự động sinh và đính kèm `X-Correlation-ID` trên HTTP Header và đẩy vào Serilog `LogContext` cùng W3C `TraceId`.<br>- Serilog Request Logging ghi nhận `UserId` định danh người dùng trên từng request.<br>- Cấu hình Redaction: Loại bỏ tuyệt đối mật khẩu, token, security stamp khỏi log; tắt sensitive query logging của EF Core.<br>- Xây dựng 3 endpoint kiểm tra sức khỏe hệ thống: `GET /health` (tổng thể), `GET /health/live` (liveness probe), `GET /health/ready` (readiness probe kiểm tra CSDL và Redis).<br><br>_**Chưa hoàn thành:**_<br>- Bổ sung OpenTelemetry Collector sink xuất dữ liệu ra hệ thống APM tập trung (kế hoạch Tuần 5). | [Program.cs](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.API/Program.cs) | **100%** |
| **7** | **[Lab Cá nhân TV1: Cache Invalidation & Fallback Resilience (Task A6/K12)] Dịch vụ Cache-Aside và kiểm thử chịu lỗi khi Cache down**<br><br>_**Đã hoàn thành:**_<br>- Triển khai dịch vụ `IRecipeCacheService` / `RecipeCacheService` phục vụ lưu trữ đệm dữ liệu công thức nấu ăn.<br>- Cài đặt mô hình Cache-Aside: Kiểm tra cache trước, nếu MISS mới truy vấn CSDL và nạp vào cache kèm thời gian sống TTL.<br>- Cài đặt cơ chế Invalidation: Hỗ trợ xóa cache theo key cụ thể (`InvalidateAsync`) hoặc theo tiền tố nhóm dữ liệu (`InvalidatePrefixAsync`) khi có thao tác cập nhật công thức.<br>- Kiểm thử độ bền Fallback Resilience: Giả lập kịch bản Cache Server (Redis/Memory) gặp sự cố; hệ thống ghi log cảnh báo và tự động fallback về gọi trực tiếp CSDL mà không làm gián đoạn trải nghiệm người dùng.<br>- Viết bài test tự động kiểm chứng trọn vẹn luồng MISS, HIT, Invalidation và Fallback Resilience.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [RecipeCacheService.cs](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.Infrastructure/RecipeCacheService.cs) | **100%** |
| **8** | **[Lab Cá nhân TV1: SEO Recipe Schema.org JSON-LD (Task A6/K19)] Dữ liệu có cấu trúc cho Google Search Rich Results**<br><br>_**Đã hoàn thành:**_<br>- Xây dựng bộ công cụ `RecipeJsonLdBuilder` sinh dữ liệu có cấu trúc Schema.org chuẩn định dạng JSON-LD.<br>- Bao gồm đầy đủ các trường thông tin theo tiêu chuẩn Google Recipe Rich Results: tên món (`name`), mô tả (`description`), ảnh đại diện (`image`), tác giả (`author`), ngày xuất bản (`datePublished`), thời gian chuẩn bị (`prepTime`), thời gian nấu (`cookTime`), tổng thời gian (`totalTime`), khẩu phần (`recipeYield`), danh mục (`recipeCategory`), danh sách nguyên liệu (`recipeIngredient`), các bước chế biến (`recipeInstructions`) và bảng dinh dưỡng (`nutrition`).<br>- Định dạng thời gian chuẩn ISO 8601 Duration (`PT30M`, `PT240M`).<br>- Tuân thủ nghiêm ngặt quy định SRS v1.1.1: Tuyệt đối không tạo dữ liệu rating giả (`aggregateRating`) khi chức năng đánh giá chưa được đưa vào phạm vi hệ thống.<br>- Viết bài test tự động kiểm tra tính hợp lệ của cấu trúc JSON sinh ra.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [RecipeJsonLd.cs](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.Application/RecipeJsonLd.cs) | **100%** |
| **9** | **[Tích hợp Đăng nhập Google & Khám phá công thức (Tích hợp TV2)] Duyệt món, lọc nhiều tiêu chí, tìm kiếm FTS tiếng Việt và Google Sign-In**<br><br>_**Đã hoàn thành:**_<br>- Nghiệm thu và tích hợp API Đăng nhập Google (`POST /api/v1/auth/google`): Xác thực token với Google SDK, tự động tạo tài khoản role Author hoặc liên kết tài khoản hiện có, cấp token phiên làm việc.<br>- Tích hợp các endpoint khám phá: `GET /api/v1/recipes` (lọc theo Category, Difficulty, CookTime, Servings, sắp xếp và phân trang) và `GET /api/v1/recipes/search` (tìm kiếm toàn văn tiếng Việt không dấu qua PostgreSQL ILike & SlugHelper).<br>- Bảo đảm an toàn thông tin: Tuyệt đối không để lộ các công thức ở trạng thái Draft hoặc Archived ra giao diện public.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Discovery.cs](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/tree/main/src/backend/CulinaryBlog.Application/Discovery.cs) | **100%** |
| **10** | **[Trách nhiệm Nhóm trưởng: Nghiệm thu Cổng G3 & Điều phối kiểm thử] Review PRs, giải quyết xung đột merge và điều phối 90 tests pass**<br><br>_**Đã hoàn thành:**_<br>- Rà soát, kiểm thử và phê duyệt PR #11 của TV2 (`feat/TV2-week2-discovery-search-google`) và PR #12 của TV4.<br>- Soạn thảo và ban hành tài liệu giải quyết xung đột: `docs/BAO_CAO_GIAI_QUYET_XUNG_DOT_MERGE_TV2_TUAN2.md`.<br>- Khắc phục lỗi encoding file migration C# và cấu hình đồng bộ locked-mode cho các dự án con.<br>- Nghiệm thu Cổng G3: Tích hợp thành công toàn bộ 34 FR của hệ thống trên nhánh `main`.<br>- Điều phối kiểm thử tự động: Đạt **90/90 tests pass 100% (Green)** trên toàn giải pháp (85 tests `CulinaryBlog.Tests` + 5 tests `ConcurrencySpike`). Chuẩn hóa mã nguồn đạt chuẩn `dotnet format --verify-no-changes`.<br><br>_**Chưa hoàn thành:**_<br>- Không có (đã hoàn thành 100% yêu cầu). | [Báo cáo xung đột merge](https://github.com/2312741-sudo/PTUDWNC-2026-Nhom16/blob/main/docs/BAO_CAO_GIAI_QUYET_XUNG_DOT_MERGE_TV2_TUAN2.md) | **100%** |

---

### Xác nhận hoàn thành

- Toàn bộ **10/10 nội dung công việc của Tuần 3 (Lab 03)** đã được thực hiện, tích hợp và kiểm chứng thực tế đạt 100% trên cơ sở dữ liệu PostgreSQL và toàn bộ 90 tests tự động.
- File báo cáo nộp bài theo chuẩn: `Lab03_2312741_NguyenThanhTam.docx` đã được tạo tại thư mục `~/Downloads/` và lưu trữ tại `docs/evidence/TV1/Lab03_2312741_NguyenThanhTam.docx`.
- File mẫu gốc `~/Downloads/Mau_Nop_Bao_Cao_Lab_Ca_Nhan_2026.docx` cũng đã được cập nhật đồng bộ.
